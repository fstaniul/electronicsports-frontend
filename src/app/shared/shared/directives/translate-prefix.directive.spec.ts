import { TranslatePrefixDirective } from './translate-prefix.directive';

describe('TransaltePrefixDirective', () => {
  it('should create an instance', () => {
    const directive = new TranslatePrefixDirective();
    expect(directive).toBeTruthy();
  });
});
