import { InputAppendDirective } from './input-append.directive';

describe('InputAppendDirective', () => {
  it('should create an instance', () => {
    const directive = new InputAppendDirective();
    expect(directive).toBeTruthy();
  });
});
