import { InputPrependDirective } from './input-prepend.directive';

describe('InputPrependDirective', () => {
  it('should create an instance', () => {
    const directive = new InputPrependDirective();
    expect(directive).toBeTruthy();
  });
});
